# Example 03: Encode and Decode Strings (LC 271)

## Problem
Design encode/decode for list of strings → single string and back.
## Key: Use length + delimiter: "3#abc5#hello"
## Approach: No delimiter characters needed — length prefix handles any chars.
**Time: O(n)  Space: O(n)**