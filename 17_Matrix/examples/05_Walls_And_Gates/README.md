# Example 05: Walls & Gates (LC 286)
## Problem
Fill each empty room (INF) with its distance to the nearest gate (0). -1 = wall.
## Approach
Multi-source BFS from all gates simultaneously.
**Time: O(m×n)  Space: O(m×n)**