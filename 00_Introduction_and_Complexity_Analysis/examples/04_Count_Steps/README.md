# Example 04: Count Algorithm Steps

## Problem
Visualize how many operations each complexity class performs for different n values.

## Input
- n values: [10, 100, 1000, 10000]

## Output
A table showing operation counts for each complexity class.

## Goal
Internalize why O(n²) becomes unacceptable at large n.