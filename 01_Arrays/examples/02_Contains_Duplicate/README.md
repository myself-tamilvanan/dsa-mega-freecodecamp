# Example 02: Contains Duplicate (LC 217)

## Problem
Return true if any value appears at least twice.

## Input
nums = [1,2,3,1]
## Output
True

## Approach
Add to set; return True if element already in set.
**Time: O(n)  Space: O(n)**