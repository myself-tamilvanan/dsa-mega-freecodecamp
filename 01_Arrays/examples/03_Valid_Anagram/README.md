# Example 03: Valid Anagram (LC 242)

## Problem
Given two strings s and t, return true if t is an anagram of s.

## Input
s = "anagram", t = "nagaram"
## Output
True

## Approach
Compare character frequency counts.
**Time: O(n)  Space: O(1)** (26 letters)